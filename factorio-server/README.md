# Notes for running Factorio

- server setting files will be generated after first run, in the configured local storage folder.
- Admins are defined in `server-adminlist.json` in the config folder. 
  - This must be created manually
  - It's just a list of usernames. Like:  `["Alloba"]`
